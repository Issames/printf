#include "main.h"

/**
 * main - Entry point
 * Description: function to print str address
 * Return: Always 0 (Success)
 */

int main(void)
{
char *str = "Hello, World!";
_printf("The address of str is: %p\n", str);

return (0);
}
